type BuyBack @entity {
  "Transaction hash-log index"
  id: ID!
  "Block number"
  block: BigInt!
  timestamp: BigInt!
  seller: Bytes!
  dai: BigInt!
  yfi: BigInt!
}

type BuyBackContract @entity {
  "Always 1."
  id: ID!
  admin: Bytes!
  treasury: Bytes!

  adminPending: Bytes
}